{
  "scripts": {
    "terraform:init": "terraform init",
    "terraform:plan": "env-path -p ./.env terraform plan",
    "terraform:apply": "env-path -p ./.env terraform apply",
    "terraform:destroy": "env-path -p ./.env terraform destroy"
  },
  "dependencies": {
    "env-path": "^1.0.7"
  }
}
